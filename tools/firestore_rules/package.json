{
  "name": "life-os-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --config ../../firebase.json --only firestore --project demo-life-os \"node --test circles.rules.test.js\""
  },
  "engines": {
    "node": "22"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.1"
  }
}
